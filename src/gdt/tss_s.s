; -------------------------------------------------
; 	加载 TR 寄存器索引
;
; 	hurley 2013/11/21
;
; -------------------------------------------------

[GLOBAL tss_flush]    ; TSS 刷新
tss_flush:
    mov ax, [esp+4]
    ltr ax            ; 加载到 TR 寄存器
    ret
